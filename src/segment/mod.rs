//! Segmentation policies: reduce a windowed sequence to continuous ranges.
//!
//! The `SegmentPolicy` trait, the run helpers (`runs`, `longest_run`), the
//! `Range` and `SegmentOptions` types, and the threshold / hysteresis built-ins
//! are implemented in a later chunk; this module is currently a
//! documentation-only placeholder.
