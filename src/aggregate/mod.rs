//! Aggregation policies: combine a window sequence into a single embedding.
//!
//! The `AggregatePolicy` trait and its built-in strategies (coverage-weighted
//! mean, mean, EMA, and saliency), the multi-vector keep-separate path, and the
//! serde policy enum are implemented in a later chunk; this module is currently
//! a documentation-only placeholder.
