//! Split policies: decide how an input is divided into windows.
//!
//! The `SplitPolicy` trait, the `FixedWindow` built-in, and the `text`-gated
//! content-aware chunker are implemented in a later chunk; this module is
//! currently a documentation-only placeholder.
